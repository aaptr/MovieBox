import { en } from '@/config/localisation/en'
import { pl } from '@/config/localisation/pl'
import { ru } from '@/config/localisation/ru'

import { LocalisationObject } from '@/types/localisationTypes'

export const localisation: Record<string, LocalisationObject> = {
  en,
  pl,
  ru
}